"""Исключения уровня хранилища, общие для ядра и интерфейса."""


class VaultError(Exception):
    """Базовая ошибка хранилища."""


class WrongPasswordError(VaultError):
    """Мастер-пароль не подошёл — расшифровать хранилище не удалось.

    Ожидается, что ``Session.unlock()`` ловит ``RuntimeError`` из
    ``core_module`` и перевыбрасывает эту ошибку. TUI отличает неверный
    пароль от настоящего сбоя (битый файл, нехватка памяти) именно по
    типу исключения, а не по тексту сообщения.
    """
